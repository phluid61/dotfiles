
# I like to open more than one file at a time in vim
alias gvim='gvim -p'
alias vim='vim -p'
alias vi='vim -p'

# Dereference symlinks when changing dirs
# (no more cwd confusion for me)
alias cd='cd -P'

